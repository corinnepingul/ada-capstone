<h2><%= t(:hello, name: @user.username) %></h2>

<% @moments.each do |moment| %>
  <div class="">
    Date: <%= moment.date %>
    Moment: <%= moment.body %>
  </div>
<% end %>

<%= link_to "Logout", logout_path, method: :delete %>
